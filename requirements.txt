# Core data + chart pipeline
pandas>=2.2
numpy>=1.26
plotly>=5.20

# Open Graph preview-image generator (src/build_og_image.py).
# Pillow is small and widely available; falls back to system fonts.
Pillow>=10.0
